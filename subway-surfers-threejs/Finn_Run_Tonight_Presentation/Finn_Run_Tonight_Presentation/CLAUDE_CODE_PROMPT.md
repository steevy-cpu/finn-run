We need to finish Finn Run tonight. Integrate the attached Finn_Run_Tonight_Presentation.zip on the existing phase2-environment branch as a small presentation-only commit. Implement it now; do not commission more art.

1. Read README.md and inspect the actual game's ready/loading/start/nickname/calibration/countdown flows before choosing insertion points. The module's example function names are placeholders.
2. Copy the hero PNG to the public asset tree; import FinnIntro.mjs and the scoped CSS. Mount once in the game pane DOM wrapper beside the canvas. Show once on page entry. Replace the visual role of the initial welcome mask without deleting IDs needed by existing logic/tests. Keep the camera panel available and its mode selector authoritative.
3. LET'S RUN must delegate to the existing New Game/start flow, preserving its permission, readiness, nickname and calibration gates. Starting from the existing camera-panel New Game button must also hide the intro. Escape and Go to game controls only dismiss it. Ensure obscured underlying game-pane controls cannot receive focus while the intro is visible; restore their original visibility/focusability on dismissal. Return focus to a useful existing control. Keep results and subsequent restart behavior unchanged. Dispose the intro at application teardown.
4. Use the optional loading/countdown/results CSS classes where they fit existing elements. Preserve the real progress and countdown timing, leaderboard updates and catch-video behavior. No new intro video, audio autoplay, runtime animation loop, framework or large dependency. Reduced motion gets a static title.
5. Keep the approved Phase 4 styling consistent. Do not alter MediaPipe settings, calibration thresholds, gestures, physics, rig, collider geometry, Arturo tuning, or existing feature choices as part of this pass. Calibration tuning is separate work.
6. The supplied art has been visually inspected and JS syntax checked; its browser preview was blocked in ChatGPT. You must inspect the actual game at its split-pane size and fullscreen. Tighten spacing if needed so the title and primary start button are immediately visible at booth size. Check equal Body/Hand labels, readable text, missing-image fallback, reduced motion, start through either button, Escape/dismiss, game over/catch and restart with no remaining intro overlay. Run the relevant existing regression checks, type-check and build.
7. Return the commit, exact changed files, a launch URL, one split-pane capture and one fullscreen capture, actual checks performed and any remaining blocker. Do not claim unperformed real-camera or booth measurements. Prioritize a working polished launch flow tonight.
